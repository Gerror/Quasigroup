#include "QuasigroupExperiments.h"

int main() {
  // Uncomment to run experiments from papers

  // QuasigroupExperiments::runAssociativityExperimentOnAbelianGroups(4, 100,250);
  // QuasigroupExperiments::runAffinityExperimentOnAbelianGroups(4, 128, 500);
  // QuasigroupExperiments::runAllAlgorithmsExperimentOnJMQuasigroups(4, 128, 500);

  return 0;
}